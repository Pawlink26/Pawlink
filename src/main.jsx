import React from 'react'
import ReactDOM from 'react-dom/client'
import RescuPawLink from './RescuPawLink.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RescuPawLink />
  </React.StrictMode>,
)
